// public/shared/theme-init.js
// Legacy initializer for pages that explicitly include this module.
// Delegates to the centralized theme controller so behaviour stays consistent.

import { initTheme } from './theme-controller.js';

initTheme();

